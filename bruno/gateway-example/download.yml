info:
  name: download
  type: http
  seq: 6

http:
  method: GET
  url: "{{gateway-host}}/api/example/download"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
