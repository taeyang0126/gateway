<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter>
  <!--
    EI_EXPOSE_REP / EI_EXPOSE_REP2：存储/返回可变对象引用。
    项目中涉及的均为 Spring DI 注入的单例 bean 或配置对象，
    外部不会持有引用修改，属于误报，全局排除。
  -->
  <Match>
    <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2"/>
  </Match>
</FindBugsFilter>
