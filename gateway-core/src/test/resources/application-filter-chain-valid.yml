gateway:
  port: 0
  routes:
    - id: api-route
      path-prefix: /api/**
      upstream: http://localhost:8080
      filters:
        - ip-access-control
        - auth
  filters:
    filter-chain-timeout-ms: 5000
    default-filters:
      - ip-access-control
