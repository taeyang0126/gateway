<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- 从 Spring Environment 读取应用名和日志目录，支持外部覆盖 -->
  <springProperty name="LOG_DIR" source="logging.file.path" defaultValue="gateway-app/logs"/>

  <!-- ===== Appenders ===== -->

  <!-- 控制台：开发时用，生产可通过 logback 配置关闭 -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %highlight(%5level) [%magenta(%thread)] %cyan(%logger{36}) : %msg%n</pattern>
    </encoder>
  </appender>

  <!-- access.log：仅收录 access logger，按天滚动，保留 30 天 -->
  <appender name="ACCESS_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/access.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/access.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>30</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%msg%n</pattern>
    </encoder>
  </appender>

  <!-- warn.log：WARN 及以上，所有 logger，按天滚动，保留 30 天 -->
  <appender name="WARN_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/warn.log</file>
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>WARN</level>
    </filter>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/warn.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>30</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %5level [%thread] %logger{36} : %msg%n</pattern>
    </encoder>
  </appender>

  <!-- error.log：ERROR 及以上，所有 logger，按天滚动，保留 30 天 -->
  <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/error.log</file>
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>ERROR</level>
    </filter>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/error.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>30</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %5level [%thread] %logger{36} : %msg%n</pattern>
    </encoder>
  </appender>

  <!-- ===== Loggers ===== -->

  <!-- access logger：写 access.log + 控制台，不向上传播（不进 warn/error.log） -->
  <logger name="access" level="INFO" additivity="false">
    <appender-ref ref="ACCESS_FILE"/>
    <appender-ref ref="CONSOLE"/>
  </logger>

  <!-- root：控制台 + warn.log + error.log -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="WARN_FILE"/>
    <appender-ref ref="ERROR_FILE"/>
  </root>

</configuration>
